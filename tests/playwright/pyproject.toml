[project]
name = "bandw-playwright-sdk"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "wandb>=0.17",
    "numpy",
    "pillow",
    "scikit-learn",
    "plotly>=6.7.0",
    "pandas>=3.0.2",
    "matplotlib",
]

[tool.uv]
# Lock file managed by uv
