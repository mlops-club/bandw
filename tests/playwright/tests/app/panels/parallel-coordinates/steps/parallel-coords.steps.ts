/**
 * Step definitions for parallel-coordinates feature files.
 *
 * All steps used by parallel-coordinates features are already defined in:
 * - steps/common.steps.ts (SDK setup, navigation, visibility)
 * - tests/app/panels/line-plot/steps/line-plot.steps.ts (workspace load, chart content, controls)
 * - tests/app/panels/scatter-plot/steps/scatter-plot.steps.ts (Add panels button, chart type options)
 *
 * This file is a placeholder for any future parallel-coordinates-specific steps.
 */
import { Given, When, Then } from '../../../../../steps/fixtures';
